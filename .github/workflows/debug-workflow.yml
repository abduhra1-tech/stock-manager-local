# 1. Edit the workflow file
nano .github/workflows/main.yml  # or use your preferred editor

# 2. Replace the rust-toolchain line with one of the options above

# 3. Commit and push
git add .github/workflows/main.yml
git commit -m "fix: update rust-toolchain action to valid reference"
git push origin main
