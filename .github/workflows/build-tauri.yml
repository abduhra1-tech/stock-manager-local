# 1. Remove ALL broken workflow files
rm -f .github/workflows/*.yml .github/workflows/*.yaml

# 2. Verify directory is empty
ls -la .github/workflows/

# 3. Create clean workflow file using GitHub Web UI (MOST RELIABLE)
# Go to: https://github.com/abduhra1-tech/stock-manager-local
# Click Actions → New workflow → set up a workflow yourself
# Name it: main.yml
# Paste ONLY this content (NO error messages):
