# 1. Check what workflow files exist
ls -la .github/workflows/

# 2. Remove the broken build-tauri.yml file
rm -f .github/workflows/build-tauri.yml

# 3. Verify only main.yml remains
ls -la .github/workflows/

# 4. Commit and push
git add -A
git commit -m "fix: remove invalid build-tauri.yml workflow file"
git push origin main
